{% extends 'base.html.twig' %}

{% block main %}
	<div class="home-container">
		{% if app.user %}
			<h1>Welcome!</h1>
			<a class='login-btn' href="{{ path('book_index') }}">See our books</a>
		{% else %}
			<h1>Welcome, login to see our books!</h1>
			<a class='login-btn' href="{{ path('login') }}">Login</a>
		{% endif %}
	</div>
{% endblock %}
